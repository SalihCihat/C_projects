#include <stdio.h>

int main(void) {
	
	int x,y:
	printf("Input x and y points: ");
	scanf("%d %d", &x ,&y);
	
	switch(x)
	    case 0: printf("the point lies on y axis");
	    break;
	    
	    default:
	
	return 0;
}
