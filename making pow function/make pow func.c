#include <stdio.h>
#include <stdlib.h>


int main() {
	
	int a, b, multi=1, i;
	
	printf("Input two number: ");
	scanf("%d %d", &a, &b);
	
	for(i==1 ; i<=b ; i+=1)
	
	multi=multi*a;
	
	printf("%d times %d is = %d", a, b, multi);
	
	return 0;
}
